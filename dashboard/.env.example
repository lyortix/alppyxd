# Aurea — AI provider keys (server-side only, never exposed to the client).
# The provider registry (src/ai/providers.ts) references these by name.
# Copy to .env.local and fill in the keys you actually use.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
DEEPSEEK_API_KEY=
OPENROUTER_API_KEY=
GROQ_API_KEY=
