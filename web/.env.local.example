# URL of the realtime backend (server/), e.g. https://your-app.up.railway.app
# Locally this defaults to http://localhost:2567 if unset.
# The frontend derives both the /presence REST calls and the Colyseus
# websocket address from this (http -> ws, https -> wss).
NEXT_PUBLIC_SERVER_URL=http://localhost:2567
