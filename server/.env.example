# Port the Colyseus realtime server listens on (default 2567).
PORT=2567

# Optional: how often outdoor maps fire an ambient world event, in ms.
# Defaults to a 3–6 minute window. Lower these for demos/testing.
# EVENT_MIN_GAP_MS=180000
# EVENT_MAX_GAP_MS=360000
